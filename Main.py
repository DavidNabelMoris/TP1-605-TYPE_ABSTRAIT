print("1. Une procédure partition alternative du tri rapide")



def assert_check(cond, message):
    if not cond:
        raise AssertionError(f"[VIOLATION] {message}")

def precondition_partition(t, p, q):
    return 0 <= p <= q < len(t)


print(assert_check(False,"shhhhhhhhhhhit"))


def 